# Ensure optional dependencies are installed.
# Needed for the platform-specific binaries the toolchain relies on
# (@napi-rs/canvas, sharp, rollup/esbuild), in CI as well as locally.
include=optional